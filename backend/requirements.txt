fastapi
uvicorn[standard]
google-genai
chromadb
mcp
google-api-python-client
google-auth
weasyprint
jinja2
python-dotenv
# zoneinfo has no bundled database: python:*-slim ships without
# /usr/share/zoneinfo, so ZoneInfo("Asia/Kolkata") raises at runtime
# unless this fallback is installed.
tzdata
certifi
httpx
